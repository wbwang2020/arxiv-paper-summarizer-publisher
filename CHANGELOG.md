# 更新日志

所有重要的项目变更都将记录在此文件中。

## v1.10.0 (2026-03-07)

### 修复问题
- 🐛 修复"仅扫描"模式仍然会自动执行知乎文章发布流程的问题
- 🐛 修复运行模式控制逻辑，确保不同模式正确执行对应的流程

### 优化改进
- ✅ 修改`run_once()`方法，添加`publish`参数来控制是否发布到知乎
- ✅ 修改`_execute_task_with_progress()`方法，根据`publish`参数决定是否发布到知乎
- ✅ 修改`scan_and_summarize()`函数，添加`publish`参数并传递给`system.run_once()`
- ✅ 优化运行模式逻辑：
  - 完整流程模式（`--run-once`）：扫描+总结+发布
  - 仅扫描模式（`--scan`）：仅扫描和总结，不发布
  - 仅发布模式（`--publish`）：仅发布已总结的论文

## v1.9.0 (2026-03-07)

### 修复问题
- 🐛 修复API密钥错误问题，确保API密钥正确传递到子进程
- 🐛 修复环境变量优先级问题，确保系统环境变量优先级高于.env文件
- 🐛 修复GUI服务器环境变量传递问题，确保.env文件正确加载
- 🐛 确保不在代码中硬编码API key和cookie，提高安全性

### 优化改进
- ✅ 统一使用 `load_dotenv(override=False)` 加载环境变量，确保系统环境变量优先级
- ✅ 优化环境变量覆盖逻辑，确保配置文件占位符正确解析
- ✅ 改进GUI服务器的环境变量传递机制

## v1.8.0 (2026-03-07)

### 新增功能
- ✅ GUI心跳机制
  - 实现定期心跳检测（每2秒一次）
  - 添加请求超时控制（3秒超时）
  - 实现心跳失败处理（5秒超时后标记断开）
  - 支持页面可见性变化处理（页面显示时立即检测）
  - 使用AbortController实现请求取消
  - 禁用缓存确保实时性
- ✅ GUI状态栏增强
  - 左侧显示系统处理状态（系统就绪/正在处理任务）
  - 右侧显示后台连接状态（后台连接正常/后台连接断开）
  - 添加状态样式（绿色-正常，蓝色-处理中，红色-断开）
  - 处理中状态添加脉冲动画效果
- ✅ GUI状态持久化机制
  - 运行控制状态持久化（运行模式、详细模式）
  - 处理状态持久化（isProcessing保存到localStorage）
  - 配置控件禁用状态持久化
  - 页面重新打开后自动恢复之前的状态
- ✅ GUI输出缓冲机制
  - 服务器端实现输出缓冲区（output_buffer）
  - 支持网页关闭后重新打开时恢复输出内容
  - 任务完成或停止时自动清空缓冲区
- ✅ GUI防重复提交机制
  - 前端检查isProcessing状态防止重复启动任务
  - 后端API检查处理状态返回400错误
- ✅ GUI配置控件禁用机制
  - 处理任务时自动禁用所有配置输入控件
  - 添加禁用样式（半透明、禁止鼠标事件）
  - 任务完成后自动启用配置控件

### 优化改进
- ✅ 使用心跳机制替代简单的轮询机制
- ✅ 优化updateProcessingUI函数，同步更新系统状态显示
- ✅ 添加import threading解决线程锁问题

## v1.7.0 (2026-03-07)

### 新增功能
- ✅ GUI服务器单实例机制优化
  - 使用独立锁端口(5001)进行单实例检查，避免与Flask服务端口(5000)冲突
  - 实现跨平台文件锁机制（Windows使用msvcrt.locking，Unix/Linux使用fcntl）
  - 添加僵尸进程检测和清理功能
  - 支持进程列表查找作为PID文件读取失败的回退机制
- ✅ GUI输出缓存清理机制
  - 添加服务器实例ID（SERVER_ID）用于检测服务器重启
  - 前端自动检测服务器重启并清除输出缓存
- ✅ 进程终止优化
  - 使用psutil递归终止所有子进程
  - 实现优雅终止（SIGTERM）和强制终止（SIGKILL）两级机制

### 修复问题
- 🐛 修复Windows系统文件锁实现问题
- 🐛 修复端口冲突导致"服务不可用"的问题
- 🐛 修复停止执行时EPIPE错误

## v1.6.0 (2026-03-06)

### 新增功能
- ✅ 实现统一输出处理模块（OutputHandler）
- ✅ 支持模块化输出配置，为每个模块提供独立的输出控制
- ✅ 新增 `ModuleOutputConfig` 和 `OutputConfig` 配置类
- ✅ 新增 `output` 配置字段，支持各模块独立输出设置
- ✅ 实现 `get_log_level` 工具函数，集中管理日志级别转换
- ✅ GUI更新：添加输出配置标签页，支持可视化配置各模块输出参数
- ✅ 支持从配置文件和GUI界面管理输出配置

### 优化改进
- ✅ 优化代码结构，移除重复的日志级别映射定义

## v1.5.0 (2026-03-04)

### 新增功能
- ✅ 实现轻量级Web GUI界面
- ✅ 支持配置文件在线编辑
- ✅ 支持论文列表查看
- ✅ 支持实时日志输出
- ✅ 实现单实例运行约束
- ✅ 所有日志信息中文化
- ✅ 章节配置外部化实现 - 将章节定义从代码移到 `config.yaml`
- ✅ 支持灵活的章节映射配置
- ✅ 新增 `SummarySectionConfig` 配置类
- ✅ 新增 `summary_sections` 配置字段
- ✅ 更新 `PaperSummarizer` 实现，支持动态章节解析
- ✅ 支持不同字段类型的解析（string, list）
- ✅ 提高系统灵活性和可扩展性
- ✅ GUI更新：AI配置分为三个标签页（基本设置、提示词设置、章节定义）
- ✅ 支持在线编辑系统提示词和提示词模板
- ✅ 支持可视化编辑章节定义（增删改查、排序）
- ✅ 新增最大输入token数配置（max_input_tokens，默认131072）

## v1.4.0 (2026-03-04)

### 新增功能
- ✅ 新增元数据配置选项（`include_metadata`）
  - `include_metadata: false`：默认不包含元数据
  - `include_metadata: true`：在Markdown文件末尾添加元数据信息
- ✅ 修改 `PaperSummary.to_markdown()` 方法，添加 `include_metadata` 参数
- ✅ 在 `StorageConfig` 类中添加 `include_metadata` 属性
- ✅ 支持通过环境变量 `STORAGE_INCLUDE_METADATA` 覆盖配置

## v1.3.0 (2026-03-03)

### 新增功能
- ✅ 重构知乎发布模块，拆分为三个独立子模块
  - `title_settings.py`: 题目设置模块
  - `publish_settings.py`: 发布设置模块
  - `content_filler.py`: 文章主体填充模块
- ✅ 新增正文填充模式配置（`content_fill_mode`）
  - 支持 `copy_paste`（拷贝粘贴）模式
  - 支持 `import_document`（导入文档）模式
- ✅ 新增调试模式配置（`debug`）
  - `debug: true`: 调试模式，输出详细日志和截图
  - `debug: false`: 执行模式，简洁输出（生产环境）

### 优化改进
- ✅ 优化编辑器填充流程：先编辑题目→再编辑发布设置→最后填充正文
- ✅ 完善字数检测机制，验证内容是否有效粘贴

## v1.2.0 (2026-03-03)

### 新增功能
- ✅ 实现Playwright浏览器自动化发布方案
- ✅ 支持超时处理机制（最多3次尝试获取文章URL）
- ✅ 支持内容长度检查（确保不少于8个字符）
- ✅ 支持发布按钮激活状态检查
- ✅ 支持Markdown解析对话框处理
- ✅ 支持下拉菜单操作优化
- ✅ 支持详细截图功能（便于调试）

### 优化改进
- ✅ 优化专栏选择逻辑，通过"专栏收录"->"发布到专栏"路径
- ✅ 实现标题长度检查和截断功能（确保不超过100字）
- ✅ 支持从现有Markdown文件直接发布

## v1.1.0 (2026-03-02)

### 新增功能
- ✅ 实现年-月文件夹结构存储 (YYYY-MM)
- ✅ 实现每个文件夹JSON格式简报 (brief.json)
- ✅ 实现最近两个月智能跳过机制
- ✅ 新增 `exists_in_recent_months()` API
- ✅ 新增 `list_recent_summaries()` API
- ✅ 新增 `get_folder_brief()` API

### 优化改进
- ✅ 优化统计功能，支持按文件夹统计

## v1.0.0 (2026-03-02)

### 核心功能
- ✅ 实现arXiv论文自动扫描
- ✅ 实现DeepSeek AI总结（20维度分析）
- ✅ 实现Markdown本地存储
- ✅ 实现知乎发布功能（API限制）
- ✅ 实现进度显示功能
- ✅ 实现环境变量配置覆盖
- ✅ 支持128k上下文长度
